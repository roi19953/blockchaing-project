var prompt = require('prompt');
 
prompt.start();

// var thisResult = "adf";


// console.log(thisResult)

// const {username, email} = await prompt.get(['username', 'email']);

// const {username, email} = await prompt.get(['username', 'email']);

